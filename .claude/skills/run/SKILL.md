# Run — web-pages-are-cheap

Start the dev server:

```bash
cd C:\Users\jguty\Projects\web-pages-are-cheap
npm run dev
```

App listens at **http://localhost:3000**.

- Wait for `✓ Ready` in the output before navigating.
- Kill with Ctrl+C when done.
- No environment variables or seed data required.
